server {
    client_max_body_size 1M;

    root /var/www/html;

    index index.html;

    location / {
        # Métodos aceitos
        allow_methods GET;
        root /var/www/html;
        index index.html;
    }
}
