server {
    listen 127.0.0.1:8080;
    listen 127.0.0.1:9090;

    client_max_body_size 1M;

    root /var/www/html;

    index index.html;

    location / {
        # Métodos aceitos
        allow_methods GET;
        root /var/www/html;
        index index.html;
    }
}
